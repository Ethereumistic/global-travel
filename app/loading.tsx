// loading.tsx

// Import the new NAMED component { Loader } from your logo file
import { Loader } from "@/components/layout/logo";

export default function Loading() {
  // This component now shows your reusable, full-screen loader
  return <Loader />;
}